# Pixel AI Server — Environment Variables
# Copy this to .env and fill in values

# Database (used by docker-compose, also readable by app)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pixelai
DATABASE_URL=postgresql://postgres:postgres@db:5432/pixelai

# Node environment
NODE_ENV=production
